import styled from 'styled-components'

export const Button = styled.button`
  background: transparent;
  border: none;
  padding-top: 8px;
  display: flex;
  align-items: center;

  & svg {
    margin-right: 8px;
  }
`
